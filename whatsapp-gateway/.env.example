PORT=3001
API_KEY=replace-with-a-long-random-string
# Your deployed dashboard's URL — needed for auto-reply to incoming messages.
# Leave blank to disable auto-reply (outgoing sends still work fine).
DASHBOARD_URL=https://your-dashboard.vercel.app
